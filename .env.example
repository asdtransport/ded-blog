# Sanity — the site fetches published content at build time.
# PUBLIC_ vars are safe to bundle. SANITY_READ_TOKEN is server-only:
# it authenticates SSG fetches and never touches the browser bundle.
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
SANITY_READ_TOKEN=
